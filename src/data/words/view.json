{
  "english": "view",
  "japanese": "ビュー",
  "description": "viewはSQLでクエリに名前を付けて、あたかもテーブルのように参照できる仮想的な結果集合です。複雑な結合や集計を再利用しやすくしたり、権限制御の単位として用いたりできます。実体を持たない通常のビューと、結果を保存するマテリアライズドビューがあります。",
  "example_natural": {
    "english": "Create a view that exposes only the columns safe for reporting.",
    "japanese": "レポーティングで公開しても安全な列だけを表すビューを作成します。"
  },
  "example_programming": [
    {
      "language": "SQL",
      "body": "CREATE VIEW active_customers AS\nSELECT id, name, email\nFROM customers\nWHERE status = 'active';"
    },
    {
      "language": "Python",
      "body": "db.execute(\"\"\"\nCREATE VIEW active_customers AS\nSELECT id, name, email FROM customers WHERE status = 'active'\n\"\"\")\nresult = db.query('SELECT * FROM active_customers')"
    },
    {
      "language": "JavaScript",
      "body": "const createView = async () => {\n  await db.raw(`CREATE VIEW active_customers AS\n    SELECT id, name, email FROM customers WHERE status = 'active'`);\n  const result = await db('active_customers').select();\n  return result;\n};"
    },
    {
      "language": "Java",
      "body": "statement.execute(\n  \"CREATE VIEW active_customers AS \" +\n  \"SELECT id, name, email FROM customers WHERE status = 'active'\"\n);\nResultSet rs = statement.executeQuery(\"SELECT * FROM active_customers\");"
    },
    {
      "language": "Visual Basic",
      "body": "Dim sql As String\nsql = \"CREATE VIEW active_customers AS \" & _\n      \"SELECT id, name, email FROM customers WHERE status = 'active'\"\ndb.Execute sql"
    }
  ],
  "themes": ["database_concepts"],
  "difficulty": 10
}
