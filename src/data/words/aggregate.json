{
  "english": "aggregate",
  "japanese": "集約",
  "description": "aggregateは複数の行の値をひとつの結果にまとめる集約演算を指します。SQLではSUMやAVG、COUNTなどの集約関数がこれに該当します。GROUP BY句やウィンドウ関数と組み合わせて用いられます。",
  "example_natural": {
    "english": "We aggregate the daily sales data into a monthly total.",
    "japanese": "日次の売上データを月次の合計に集約します。"
  },
  "example_programming": [
    {
      "language": "SQL",
      "body": "-- aggregate\nSELECT department, SUM(salary) AS total_salary\nFROM employees\nGROUP BY department;"
    },
    {
      "language": "JavaScript",
      "body": "const sales = [{dept: 'A', amount: 100}, {dept: 'A', amount: 150}, {dept: 'B', amount: 200}];\nconst aggregate = sales.reduce((acc, s) => {\n  acc[s.dept] = (acc[s.dept] || 0) + s.amount;\n  return acc;\n}, {});"
    },
    {
      "language": "Visual Basic",
      "body": "Dim totals As New Dictionary(Of String, Double)\nFor Each item In dataList\n    If Not totals.ContainsKey(item.Category) Then\n        totals.Add(item.Category, 0)\n    End If\n    totals(item.Category) += item.Value\nNext"
    },
    {
      "language": "Python",
      "body": "from itertools import groupby\ndata = [('A', 100), ('A', 150), ('B', 200)]\naggregate = {k: sum(v for _, v in g) for k, g in groupby(sorted(data), key=lambda x: x[0])}"
    },
    {
      "language": "Java",
      "body": "Map<String, Integer> aggregate = sales.stream()\n  .collect(Collectors.groupingBy(\n    Sale::getDept,\n    Collectors.summingInt(Sale::getAmount)\n  ));"
    }
  ],
  "themes": ["sql_aggregation"],
  "difficulty": 10
}
