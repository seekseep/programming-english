{
  "english": "timeout",
  "japanese": "タイムアウト",
  "description": "タイムアウトは一定時間経過後に処理を実行したり中断したりする仕組みです。JavaScriptのsetTimeoutは指定ミリ秒後にコールバックを一度だけ実行し、通信の制限時間設定などにも使われます。",
  "example_natural": {
    "english": "Abort the request if it exceeds the timeout.",
    "japanese": "リクエストがタイムアウトを超えたら中断してください。"
  },
  "example_programming": [
    {
      "language": "JavaScript",
      "body": "const controller = new AbortController();\nsetTimeout(() => controller.abort(), 3000);\nconst res = await fetch('/api/slow', { signal: controller.signal });"
    },
    {
      "language": "Python",
      "body": "import socket\nsocket.setdefaulttimeout(5)\ntry:\n  response = socket.create_connection(('example.com', 80), timeout=3)\nexcept socket.timeout:\n  print('timeout occurred')"
    },
    {
      "language": "Java",
      "body": "try {\n  URLConnection conn = url.openConnection();\n  conn.setReadTimeout(3000);\n  conn.setConnectTimeout(3000);\n  conn.getInputStream();\n} catch (SocketTimeoutException e) {}"
    },
    {
      "language": "Visual Basic",
      "body": "Application.Wait Now + TimeValue(\"0:00:03\")\nMsgBox \"timeout reached\""
    },
    {
      "language": "SQL",
      "body": "SET LOCK_TIMEOUT 3000;\nBEGIN TRANSACTION;\nUPDATE users SET status = 'active';\nCOMMIT;"
    }
  ],
  "themes": ["async_programming"],
  "difficulty": 6
}
