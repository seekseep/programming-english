{
  "english": "constraint",
  "japanese": "制約",
  "description": "constraint は「制約」という意味で、データベースではテーブルのカラムに課すルール（NOT NULL、UNIQUE、CHECK、FOREIGN KEY など）を指します。データの整合性を保つために使われます。",
  "example_natural": {
    "english": "Budget constraints forced us to postpone the project.",
    "japanese": "予算の制約で私たちはプロジェクトを延期せざるを得なかった。"
  },
  "example_programming": [
    {
      "language": "SQL",
      "body": "CREATE TABLE users (\n  id INTEGER PRIMARY KEY,\n  email TEXT NOT NULL,\n  age INTEGER,\n  CONSTRAINT uq_email UNIQUE (email),\n  CONSTRAINT chk_age CHECK (age >= 0)\n);"
    },
    {
      "language": "JavaScript",
      "body": "const schema = {\n  email: { type: 'string', required: true },\n  age: { type: 'number', min: 0, max: 150 }\n};"
    },
    {
      "language": "Python",
      "body": "from pydantic import BaseModel, Field\nclass User(BaseModel):\n  email: str\n  age: int = Field(ge=0, le=150)"
    },
    {
      "language": "Java",
      "body": "class User {\n  @NotNull\n  private String email;\n  @Min(0)\n  @Max(150)\n  private int age;\n}"
    },
    {
      "language": "Visual Basic",
      "body": "Private Sub ValidateConstraint(age As Integer)\n  If age < 0 Or age > 150 Then\n    Err.Raise 5\n  End If\nEnd Sub"
    }
  ],
  "themes": ["database_concepts"],
  "difficulty": 9
}
